# Pinned to a major version per dependency. Unpinned, `anthropic` alone would
# install whatever is current, and the 0.x to 1.x jump was a breaking change
# (httpx2, awaited async raw responses, removed deprecated parameters). Anyone
# cloning this months from now should get a working install, not a surprise.
# Verified against the versions in the comments on 2026-09-12.

anthropic>=1.5,<2        # tested at 1.5.0
flask>=3.1,<4            # tested at 3.1.3
requests>=2.32,<3        # tested at 2.34.2
python-dotenv>=1.0,<2    # tested at 1.2.3
gunicorn>=26,<27         # tested at 26.2.0
tenacity>=9,<10          # tested at 9.1.4
structlog>=26,<27        # tested at 26.1.0
pydantic>=2.13,<3        # tested at 2.13.5
