[tool.pytest.ini_options]
# Put the repo root on sys.path so tests can import the `core` and `evals`
# packages whether pytest is invoked as `pytest` or `python -m pytest`.
pythonpath = ["."]
testpaths = ["tests"]
